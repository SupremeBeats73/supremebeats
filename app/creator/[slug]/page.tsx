"use client";

import PlaceholderPage from "../../components/PlaceholderPage";

export default function PublicCreatorPage() {
  return (
    <div className="min-h-screen bg-[var(--background)] px-4 py-16">
      <PlaceholderPage
        title="Creator profiles"
        description="Public creator profiles, pinned tracks, and social features will ship in a later phase. For now, focus is on your private dashboard, studio, and projects."
      />
    </div>
  );
}

